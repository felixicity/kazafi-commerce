#1. API URL for Client-Side Fetching (Exposed to the browser)
# This is the public URL Next.js fetches from. In development, it points to the
# Next.js server's public URL (which will proxy the request).
NEXT_PUBLIC_API_BASE_URL = http://localhost:3001

# 2. API URL for Server-Side Fetching (NOT exposed to the browser)
# This is the internal URL used by Next.js Server Components or Route Handlers
# (server-to-server calls).
EXPRESS_API_INTERNAL_URL = http://localhost:5000